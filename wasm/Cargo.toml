[package]
name = "proofcodec-verify-wasm"
version = "0.1.0"
edition = "2021"
description = "ProofCodec residual decoder — WASM build (decode-only, MIT licensed)"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
